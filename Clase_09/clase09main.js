// let conditionNumber = 10;
// while (conditionNumber < 0) {
    // código que quiero ejecutar
    // en cada iteracion
// conditionNumber = conditionNumber - 1;
// }


const tabladeMultiplicar = 3;
// (number++) number = number + 1;
for (let number = 1; number < 11; number++) {
    // código que quiero ejecutar
    // en cada iteracion   
     const resultado = tabladeMultiplicar * number;
    // console.log(tabladeMultiplicar + ' x ' + number + ' = ' + resultado);
    // otro modo de escribir la linea anterior
    console.log(`${tabladeMultiplicar} x ${number} = ${resultado}`);
    }
